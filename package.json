{
  "name": "sivion-enterprise-hub-root",
  "version": "1.0.0",
  "description": "Monorepo root — Backend deployed via Render using Backend/ as root directory",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix Backend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
